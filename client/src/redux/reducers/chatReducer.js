const initialState = {
  id: '123123',
  pic: null,
  title: 'some title'
}

const chatReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default chatReducer
