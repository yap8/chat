const initialState = {
  id: '123123',
  name: 'some name',
  avatar: null
}

const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default userReducer
